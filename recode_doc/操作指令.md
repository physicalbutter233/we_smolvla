让机械臂掉使能：
```python
python scripts/disable_so101_follower_torque.py --port /dev/ttyACM0 --yes
```

同步推理评估：
```python
export CHECKPOINT=/path/to/we_smolvla/outputs/train/smolvla_so101_original_step_40000_batchsize_16/checkpoints/040000/pretrained_model \
export repo_id=eval_so101_pick_blue_cube_bs_16_ckp_40000_n_5_max_30

lerobot-record   --robot.type=so101_follower   --robot.port=/dev/ttyACM0 --robot.id=my_awesome_follower_arm --robot.cameras="{ camera1: {type: opencv, index_or_path: /dev/video0, width: 640, height: 480, fps: 30}, camera2: {type: opencv, index_or_path: /dev/video2, width: 640, height: 480, fps: 30} }"   --robot.max_relative_target=30   --policy.path="${CHECKPOINT}"   --policy.device=cuda   --policy.n_action_steps=10   --policy.empty_cameras=1   --dataset.repo_id=local/"${repo_id}"_$(date +%m%d_%H%M)~   --dataset.root=/path/to/we_smolvla/datasets/eval/"${repo_id}"_$(date +%m%d_%H%M)   --dataset.single_task="Pick up the blue cube and place it in the box."   --dataset.fps=30   --dataset.num_episodes=3   --dataset.episode_time_s=30   --dataset.reset_time_s=15   --dataset.video=true   --dataset.video_encoding_batch_size=1   --dataset.num_image_writer_threads_per_camera=4   --dataset.push_to_hub=false   --display_data=true  --display_action_trace=true
```

^c58e20

使用特定数据集进行训练：
```python
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
export TOKENIZERS_PARALLELISM=false
export HF_HOME=$HOME/.cache/huggingface
export HF_LEROBOT_HOME=$HOME/.cache/huggingface/lerobot

export BATCH_SIZE=16
export STEPS=100000
export SAVE_EVERY=2000

lerobot-train \
  --policy.path=/path/to/we_smolvla/models/smolvla_base \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --policy.freeze_vision_encoder=true \
  --policy.train_expert_only=true \
  --policy.train_state_proj=true \
  --policy.empty_cameras=1 \
  --dataset.repo_id=local/so101_pick_blue_cube_918 \
  --dataset.root=/path/to/we_smolvla/datasets/raw/so101_pick_blue_cube_918 \
  --dataset.video_backend=torchcodec \
  --output_dir=/path/to/we_smolvla/outputs/train/smolvla_so101_pick_blue_cube_918 \
  --job_name=smolvla_so101_pick_blue_cube_918 \
  --batch_size=${BATCH_SIZE} \
  --steps=${STEPS} \
  --num_workers=4 \
  --log_freq=50 \
  --eval_freq=0 \
  --save_freq=${SAVE_EVERY} \
  --policy.use_amp=true \
  --wandb.enable=true \
  --wandb.project=smolvla-so101 \
  --wandb.disable_artifact=true
```

^a08973

录制数据集
```python
# 测试是否正常使能，首先要测试FOLLOW/LEADER是0/1
lerobot-teleoperate \
  --robot.type=so101_follower \
  --robot.port=/dev/ttyACM0 \
  --robot.id=my_awesome_follower_arm \
  --robot.cameras='{}' \
  --teleop.type=so101_leader \
  --teleop.port=/dev/ttyACM1 \
  --teleop.id=my_awesome_leader_arm
```

```python
# 正式录制，首先要测试FOLLOW/LEADER是0/1
lerobot-record \
  --robot.type=so101_follower \
  --robot.port=/dev/ttyACM1 \
  --robot.id=my_awesome_follower_arm \
  --robot.cameras="{ camera1: {type: opencv, index_or_path: /dev/video0, width: 640, height: 480, fps: 30}, camera2: {type: opencv, index_or_path: /dev/video2, width: 640, height: 480, fps: 30} }" \
  --teleop.type=so101_leader \
  --teleop.port=/dev/ttyACM0 \
  --teleop.id=my_awesome_leader_arm \
  --dataset.repo_id=local/so101_pick_blue_cube_pilot \
  --dataset.root=/path/to/we_smolvla/datasets/raw/so101_pick_blue_cube_pilot \
  --dataset.single_task="Pick up the blue cube and place it in the box." \
  --dataset.fps=30 \
  --dataset.num_episodes=10 \
  --dataset.episode_time_s=30 \
  --dataset.reset_time_s=15 \
  --dataset.video=true \
  --dataset.video_encoding_batch_size=1 \
  --dataset.num_image_writer_threads_per_camera=4 \
  --dataset.push_to_hub=false \
  --display_data=true
```

^bd740a

可视化某条轨迹：
```python
lerobot-dataset-viz   --repo-id local/so101_pick_blue_cube   --root /path/to/we_smolvla/datasets/raw/so101_pick_blue_cube   --episode-index 10   --batch-size 10   --num-workers 0
```

异步推理命令：
```python
# terminal_1
python -m lerobot.async_inference.policy_server \
  --host=127.0.0.1 \
  --port=8080

# terminal_2，其中,display_action_trace可以记录action chunk的队列长度&六个自由度舵机的实际数据（实线）与预测数据（虚线）
python -m lerobot.async_inference.robot_client \
  --server_address=127.0.0.1:8080 \
  --robot.type=so101_follower \
  --robot.port=/dev/ttyACM0 \
  --robot.id=my_awesome_follower_arm \
  --robot.cameras="{ camera1: {type: opencv, index_or_path: /dev/video0, width: 640, height: 480, fps: 30}, camera2: {type: opencv, index_or_path: /dev/video2, width: 640, height: 480, fps: 30} }" \
  --policy_type=smolvla \
  --pretrained_name_or_path="${CHECKPOINT}" \
  --policy_device=cuda \
  --actions_per_chunk=50 \
  --chunk_size_threshold=0.5 \
  --aggregate_fn_name=weighted_average \
  --display_data=true \
  --display_action_trace=true
```

^7ac8bb
